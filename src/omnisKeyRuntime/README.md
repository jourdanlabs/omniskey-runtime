# OMNIS KEY Runtime

Standalone OMNIS KEY verified runtime wrapper over the shared verified runtime core.

```sh
mirrorverse omniskey-runtime status .
mirrorverse omniskey-runtime ask "what can you verify?"
mirrorverse omniskey-runtime telegram-poll . --json
```
